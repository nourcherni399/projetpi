{% extends 'layouts/user.html.twig' %}

{% block title %}{{ categorie.name }} - Blog AutiCare{% endblock %}

{% block user_content %}
<section class="mb-10">
    <div class="flex flex-col md:flex-row md:items-center gap-6 mb-6">
        <div class="flex items-center gap-4">
            <div class="w-16 h-16 rounded-xl overflow-hidden border-2 border-[#E5E0D8]">
                {% if categorie.image %}
                    <img src="{{ asset(categorie.image) }}" alt="{{ categorie.name }}" class="w-full h-full object-cover">
                {% else %}
                    <div class="w-full h-full bg-[#F5F1EB] flex items-center justify-center">
                        <svg class="w-8 h-8 text-[#A7C7E7]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
                        </svg>
                    </div>
                {% endif %}
            </div>
            <div>
                <h1 class="text-3xl font-bold text-[#4B5563]">{{ categorie.name }}</h1>
                <p class="text-[#6B7280]">{{ modules|length }} module{{ modules|length > 1 ? 's' : '' }} d'apprentissage</p>
            </div>
        </div>
        <div class="md:ml-auto">
            <a href="{{ path('user_blog') }}" class="inline-flex items-center gap-2 px-4 py-2.5 rounded-lg border border-[#E5E0D8] text-[#4B5563] font-medium hover:bg-[#F5F1EB] transition-colors">
                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                </svg>
                Retour aux catégories
            </a>
        </div>
    </div>
    
    <div class="rounded-2xl p-6">
        <p class="text-[#6B7280] mb-6 text-center">
            Découvrez tous les modules d'apprentissage de la catégorie <span class="font-semibold text-[#4B5563]">{{ categorie.name }}</span>. 
            Chaque module contient des ressources et des espaces de discussion.
        </p>
        
        {% if modules|length > 0 %}
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {% for module in modules %}
            <article class="relative rounded-3xl overflow-hidden bg-[#1F1F1F] aspect-square w-full flex flex-col group shadow-[0_10px_30px_rgba(0,0,0,0.18),0_24px_55px_rgba(0,0,0,0.22)] ring-1 ring-white/20">
                {% if module.image %}
                    <img src="{{ asset(module.image) }}" alt="{{ module.titre }}" class="absolute inset-0 w-full h-full object-cover transition-transform duration-500 group-hover:scale-105">
                {% else %}
                    <div class="absolute inset-0 bg-gradient-to-br from-[#3F3C35] to-[#1F1F1F]"></div>
                {% endif %}

                <div class="absolute inset-0 bg-black/30"></div>
                <div class="absolute inset-0 bg-gradient-to-t from-black/78 via-black/35 to-black/10"></div>

                <div class="relative z-10 p-5 md:p-6 h-full text-white">
                    <div class="absolute top-5 left-5 md:top-6 md:left-6">
                        <span class="inline-flex px-3 py-1 rounded-full text-[10px] font-semibold bg-white/30 backdrop-blur text-white">
                            {{ module.niveau|capitalize }}
                        </span>
                    </div>

                    <div class="h-full flex flex-col justify-end">
                        <div class="mb-2 text-[10px] text-white/85 uppercase tracking-wide">
                            {{ module.categorie ? module.categorie.label : 'Non défini' }}
                        </div>

                        <h3 class="text-xl md:text-2xl leading-tight font-bold mb-3 line-clamp-2">
                            {{ module.titre }}
                        </h3>

                        <p class="text-white/75 text-xs md:text-sm leading-relaxed mb-2 line-clamp-3">
                            {{ module.description ?: 'Module sans description.' }}
                        </p>

                        <div class="mt-2 flex items-center justify-between gap-4">
                            <a href="{{ path('user_blog_module', { id: module.id }) }}"
                               class="flex-1 inline-flex items-center justify-center gap-2 px-5 py-3 rounded-2xl bg-white/25 hover:bg-white/35 text-white text-lg font-semibold transition-colors">
                                Lire
                            </a>
                            {% if app.user %}
                                {% set isSaved = module.id in (savedModuleIds|default([])) %}
                                <form method="POST" action="{{ path('favoris_module_toggle', { id: module.id }) }}">
                                    <input type="hidden" name="_token" value="{{ csrf_token('toggle_module_favori_' ~ module.id) }}">
                                    <button type="submit"
                                            aria-label="{{ isSaved ? 'Retirer des enregistrements' : 'Enregistrer le module' }}"
                                            class="inline-flex items-center justify-center w-12 h-12 rounded-2xl transition-colors {{ isSaved ? 'bg-[#FF6B00] text-white hover:bg-[#ff7f26]' : 'bg-white/20 text-white hover:bg-white/30' }}">
                                        <svg class="w-5 h-5" fill="{{ isSaved ? 'currentColor' : 'none' }}" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.2">
                                            <path stroke-linecap="round" stroke-linejoin="round" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3-7 3V5z" />
                                        </svg>
                                    </button>
                                </form>
                            {% else %}
                                <a href="{{ path('app_login') }}"
                                   aria-label="Se connecter pour enregistrer"
                                   class="inline-flex items-center justify-center w-12 h-12 rounded-2xl bg-white/20 hover:bg-white/30 text-white transition-colors">
                                    <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.2">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3-7 3V5z" />
                                    </svg>
                                </a>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </article>
            {% endfor %}
        </div>
        {% else %}
        <div class="text-center py-12">
            <svg class="w-16 h-16 mx-auto text-gray-400 mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
            <h3 class="text-lg font-semibold text-[#4B5563] mb-2">Aucun module disponible</h3>
            <p class="text-[#6B7280] mb-6">Il n'y a pas encore de modules dans cette catégorie.</p>
            <a href="{{ path('user_blog') }}" class="inline-flex items-center gap-2 px-4 py-2.5 rounded-lg bg-[#A7C7E7] text-white font-medium hover:bg-[#B8D4ED] transition-colors">
                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                </svg>
                Retour aux catégories
            </a>
        </div>
        {% endif %}
    </div>
</section>
{% endblock %}