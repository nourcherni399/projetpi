{% extends 'layouts/user.html.twig' %}

{% block title %}{{ categorie.name }} - Blog AutiCare{% endblock %}

{% block user_content %}
<section class="mb-10">
    <div class="flex flex-col md:flex-row md:items-center gap-6 mb-6">
        <div class="flex items-center gap-4">
            <div class="w-16 h-16 rounded-xl overflow-hidden border-2 border-[#E5E0D8]">
                {% if categorie.image %}
                    <img src="{{ asset(categorie.image) }}" alt="{{ categorie.name }}" class="w-full h-full object-cover">
                {% else %}
                    <div class="w-full h-full bg-gradient-to-br from-[#A7C7E7]/20 to-[#95d7b5]/20 flex items-center justify-center">
                        <svg class="w-8 h-8 text-[#A7C7E7]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
                        </svg>
                    </div>
                {% endif %}
            </div>
            <div>
                <h1 class="text-3xl font-bold text-[#4B5563]">{{ categorie.name }}</h1>
                <p class="text-[#6B7280]">{{ modules|length }} module{{ modules|length > 1 ? 's' : '' }} d'apprentissage</p>
            </div>
        </div>
        <div class="md:ml-auto">
            <a href="{{ path('user_blog') }}" class="inline-flex items-center gap-2 px-4 py-2.5 rounded-lg bg-[#A7C7E7]/10 text-[#A7C7E7] font-medium hover:bg-[#A7C7E7]/20 transition-colors">
                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                </svg>
                Retour aux catégories
            </a>
        </div>
    </div>
    
    <div class="bg-gradient-to-br from-[#A7C7E7]/10 to-[#95d7b5]/10 rounded-2xl border border-[#A7C7E7]/20 p-6">
        <p class="text-[#6B7280] mb-6 text-center">
            Découvrez tous les modules d'apprentissage de la catégorie <span class="font-semibold text-[#4B5563]">{{ categorie.name }}</span>. 
            Chaque module contient des ressources et des espaces de discussion.
        </p>
        
        {% if modules|length > 0 %}
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {% for module in modules %}
            <article class="bg-white rounded-xl shadow-lg border border-[#E5E0D8]/50 overflow-hidden hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
                <!-- Image du module -->
                <div class="aspect-[16/10] w-full overflow-hidden bg-gradient-to-br from-[#A7C7E7]/20 to-[#95d7b5]/20 relative">
                    {% if module.image %}
                        <img src="{{ asset(module.image) }}" alt="{{ module.titre }}" class="w-full h-full object-cover">
                    {% else %}
                        <div class="w-full h-full flex flex-col items-center justify-center text-[#4B5563] p-4">
                            <div class="w-12 h-12 rounded-full bg-white/50 flex items-center justify-center mb-2">
                                <svg class="w-6 h-6 text-[#A7C7E7]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
                                </svg>
                            </div>
                            <span class="text-sm font-medium">Module</span>
                        </div>
                    {% endif %}

                    <!-- Badge de niveau -->
                    <div class="absolute top-3 right-3">
                        <span class="px-2 py-1 rounded-full text-xs font-medium
                            {% if module.niveau == 'facile' %}bg-green-100 text-green-800
                            {% elseif module.niveau == 'moyen' %}bg-yellow-100 text-yellow-800
                            {% else %}bg-red-100 text-red-800{% endif %}">
                            {{ module.niveau|capitalize }}
                        </span>
                    </div>
                    
                    <!-- Badge catégorie -->
                    <div class="absolute top-3 left-3">
                        <span class="px-2 py-1 rounded-full text-xs font-medium bg-white/90 text-[#4B5563] backdrop-blur-sm">
                            {{ module.categorie ? module.categorie.label : 'Non défini' }}
                        </span>
                    </div>
                </div>

                <!-- Contenu du module -->
                <div class="p-5">
                    <h3 class="text-lg font-bold text-[#4B5563] mb-3 line-clamp-2">{{ module.titre }}</h3>
                    <p class="text-[#6B7280] text-sm mb-4 line-clamp-3">{{ module.description }}</p>

                    <!-- Métadonnées -->
                    <div class="flex items-center justify-between pt-3 border-t border-[#E5E0D8]/50">
                        <div class="flex items-center gap-3 text-xs text-[#6B7280]">
                            <div class="flex items-center gap-1">
                                <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                                </svg>
                                {{ module.dateCreation ? module.dateCreation|date('d/m/Y') : 'N/A' }}
                            </div>
                            <div class="flex items-center gap-1">
                                <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                                </svg>
                                {{ module.blogs|length }} article{{ module.blogs|length > 1 ? 's' : '' }}
                            </div>
                        </div>
                        <a href="{{ path('user_blog_module', { id: module.id }) }}"
                           class="inline-flex items-center gap-1 px-3 py-1.5 rounded-lg bg-[#A7C7E7] text-white text-sm font-medium hover:bg-[#B8D4ED] transition-colors">
                            <span>Explorer</span>
                            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
                            </svg>
                        </a>
                    </div>
                </div>
            </article>
            {% endfor %}
        </div>
        {% else %}
        <div class="text-center py-12">
            <svg class="w-16 h-16 mx-auto text-gray-400 mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
            <h3 class="text-lg font-semibold text-[#4B5563] mb-2">Aucun module disponible</h3>
            <p class="text-[#6B7280] mb-6">Il n'y a pas encore de modules dans cette catégorie.</p>
            <a href="{{ path('user_blog') }}" class="inline-flex items-center gap-2 px-4 py-2.5 rounded-lg bg-[#A7C7E7] text-white font-medium hover:bg-[#B8D4ED] transition-colors">
                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                </svg>
                Retour aux catégories
            </a>
        </div>
        {% endif %}
    </div>
</section>
{% endblock %}