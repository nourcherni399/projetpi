{% extends 'layouts/user.html.twig' %}

<<<<<<< HEAD
{% block title %}{{ event.title }} - AutiCare{% endblock %}
=======
{% block title %}{{ evenement.title }} - AutiCare{% endblock %}
>>>>>>> 72089269acfd37b80d1154606c1f9a5afd193770

{% block user_content %}
<a href="{{ path('user_events') }}" class="inline-flex items-center gap-2 text-[#6B7280] hover:text-[#A7C7E7] mb-6 focus:outline focus:ring-2 focus:ring-[#A7C7E7] rounded">
    <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18" /></svg>
    Retour aux événements
</a>
<<<<<<< HEAD

<div class="flex flex-col lg:flex-row gap-8 mb-12">
    <div class="flex-1 min-w-0">
        <div class="flex flex-wrap gap-2 mb-3">
            <span class="px-2 py-1 rounded-md text-xs font-medium bg-[#95d7b5]/40 text-[#4B5563]">{{ event.type_tag }}</span>
            {% for tag in event.public_tags %}
            <span class="px-2 py-1 rounded-md text-xs font-medium bg-[#E5E0D8] text-[#4B5563]">{{ tag }}</span>
            {% endfor %}
        </div>
        <h1 class="text-2xl md:text-3xl font-bold text-[#4B5563] mb-4">{{ event.title }}</h1>
        <div class="flex flex-wrap gap-4 text-[#6B7280] text-sm mb-6">
            <span class="flex items-center gap-1"><svg class="w-5 h-5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 012.25-2.25h13.5A2.25 2.25 0 0121 7.5v11.25m-18 0A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75m-18 0v-7.5A2.25 2.25 0 015.25 9h13.5A2.25 2.25 0 0121 11.25v7.5" /></svg> {{ event.date_label }}</span>
            <span class="flex items-center gap-1"><svg class="w-5 h-5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" /></svg> {{ event.time_range }}</span>
            <span class="flex items-center gap-1"><svg class="w-5 h-5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" /></svg> {{ event.location }}</span>
        </div>
        <div class="aspect-video bg-[#E5E0D8] rounded-2xl flex items-center justify-center text-[#6B7280] mb-8">Image de l'événement</div>

        <section class="mb-8">
            <h2 class="text-xl font-bold text-[#4B5563] mb-4">À propos de cet événement</h2>
            <p class="text-[#6B7280] mb-4">{{ event.description_long }}</p>
        </section>

        {% if event.programme|length > 0 %}
        <section class="mb-8">
            <h2 class="text-xl font-bold text-[#4B5563] mb-4">Programme</h2>
            <ol class="list-decimal list-inside space-y-2 text-[#6B7280]">
                {% for item in event.programme %}
                <li><span class="font-medium text-[#4B5563]">{{ item.label }}</span> <span class="text-sm">({{ item.duration }})</span></li>
                {% endfor %}
            </ol>
=======
{% for message in app.flashes('success') %}
    <div class="mb-4 p-3 rounded-lg bg-green-50 border border-green-200 text-green-800 text-sm">{{ message }}</div>
{% endfor %}
{% for message in app.flashes('error') %}
    <div class="mb-4 p-3 rounded-lg bg-red-50 border border-red-200 text-red-800 text-sm">{{ message }}</div>
{% endfor %}
{% for message in app.flashes('info') %}
    <div class="mb-4 p-3 rounded-lg bg-blue-50 border border-blue-200 text-blue-800 text-sm">{{ message }}</div>
{% endfor %}

{% if evenement.thematique and evenement.thematique.image %}
{% set eventImg = evenement.thematique.image starts with 'http' ? evenement.thematique.image : asset(evenement.thematique.image) %}
<div class="rounded-2xl overflow-hidden mb-8 aspect-[21/9] max-h-64 bg-[#E5E0D8]">
    <img src="{{ eventImg }}" alt="{{ evenement.title }}" class="w-full h-full object-cover" loading="lazy">
</div>
{% endif %}
<div class="flex flex-col lg:flex-row gap-8 mb-12">
    <div class="flex-1 min-w-0">
        {% if evenement.thematique %}
        <span class="inline-block px-2 py-1 rounded-md text-xs font-medium bg-[#95d7b5]/40 text-[#4B5563] mb-3">{{ evenement.thematique.nomThematique }}</span>
        {% endif %}
        <h1 class="text-2xl md:text-3xl font-bold text-[#4B5563] mb-4">{{ evenement.title }}</h1>
        <div class="flex flex-wrap gap-4 text-[#6B7280] text-sm mb-6">
            <span class="flex items-center gap-1"><svg class="w-5 h-5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 012.25-2.25h13.5A2.25 2.25 0 0121 7.5v11.25m-18 0A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75m-18 0v-7.5A2.25 2.25 0 015.25 9h13.5A2.25 2.25 0 0121 11.25v7.5" /></svg> {{ evenement.dateEvent ? evenement.dateEvent|date('d/m/Y') : '—' }}</span>
            <span class="flex items-center gap-1"><svg class="w-5 h-5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" /></svg> {{ evenement.heureDebut ? evenement.heureDebut|date('H:i') : '' }} – {{ evenement.heureFin ? evenement.heureFin|date('H:i') : '' }}</span>
            <span class="flex items-center gap-1"><svg class="w-5 h-5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" /></svg> {{ evenement.lieu|default('—') }}</span>
        </div>

        {% if evenement.description %}
        <section class="mb-8">
            <h2 class="text-xl font-bold text-[#4B5563] mb-4">À propos de cet événement</h2>
            <p class="text-[#6B7280] whitespace-pre-wrap">{{ evenement.description }}</p>
>>>>>>> 72089269acfd37b80d1154606c1f9a5afd193770
        </section>
        {% endif %}
    </div>

    <aside class="lg:w-96 shrink-0">
        <div class="bg-white rounded-2xl shadow-sm border border-[#E5E0D8] p-6 sticky top-4 space-y-6">
            <div>
<<<<<<< HEAD
                <p class="text-2xl font-bold text-[#4B5563]">{{ event.price_display }}</p>
                <p class="text-sm text-[#6B7280]">par participant</p>
            </div>
            <div>
                <p class="text-[#4B5563] font-medium mb-2">{{ event.places_remaining }} places restantes sur {{ event.capacity_max }}</p>
                <div class="h-2 bg-[#E5E0D8] rounded-full overflow-hidden">
                    <div class="h-full bg-[#A7C7E7] rounded-full" style="width: {{ (event.capacity_current / event.capacity_max * 100)|round }}%"></div>
                </div>
            </div>
            <a href="#" class="block w-full text-center px-6 py-3 rounded-lg bg-[#A7C7E7] text-white font-medium hover:bg-[#B8D4ED] focus:outline focus:ring-2 focus:ring-[#A7C7E7]">S'inscrire à l'événement</a>
            <div class="flex gap-2">
                <button type="button" class="flex-1 flex items-center justify-center gap-2 px-4 py-2.5 rounded-lg border border-[#E5E0D8] text-[#6B7280] text-sm font-medium hover:bg-[#F5F1EB] focus:outline focus:ring-2 focus:ring-[#A7C7E7]">
                    <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" /></svg>
                    Sauvegarder
                </button>
                <button type="button" class="flex-1 flex items-center justify-center gap-2 px-4 py-2.5 rounded-lg border border-[#E5E0D8] text-[#6B7280] text-sm font-medium hover:bg-[#F5F1EB] focus:outline focus:ring-2 focus:ring-[#A7C7E7]">
                    <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z" /></svg>
                    Partager
                </button>
            </div>
            <div>
                <h3 class="font-semibold text-[#4B5563] mb-2">Lieu</h3>
                <p class="text-[#6B7280] text-sm">{{ event.location }}</p>
                <p class="text-[#6B7280] text-sm">{{ event.address_full }}</p>
                <div class="mt-3 h-32 bg-[#E5E0D8] rounded-xl flex items-center justify-center text-[#6B7280] text-sm">Carte</div>
            </div>
            <div>
                <h3 class="font-semibold text-[#4B5563] mb-2">Une question ?</h3>
                <a href="#" class="text-[#A7C7E7] font-medium hover:underline">{{ event.contact_label }}</a>
            </div>
=======
                <h3 class="font-semibold text-[#4B5563] mb-2">Lieu</h3>
                <p class="text-[#6B7280] text-sm">{{ evenement.lieu|default('Non précisé') }}</p>
            </div>
            {% if not app.user %}
                <p class="text-[#6B7280] text-sm">Connectez-vous pour vous inscrire à cet événement.</p>
                <a href="{{ path('app_login') }}" class="block w-full text-center px-6 py-3 rounded-lg bg-[#A7C7E7] text-white font-medium hover:bg-[#B8D4ED] focus:outline focus:ring-2 focus:ring-[#A7C7E7]">Se connecter</a>
            {% elseif userInscrit|default(false) %}
                <p class="text-emerald-600 font-medium text-sm">Vous êtes inscrit à cet événement.</p>
                <form method="post" action="{{ path('user_event_unregister', { id: evenement.id }) }}" onsubmit="return confirm('Se désinscrire de cet événement ?');">
                    <input type="hidden" name="_token" value="{{ csrf_token('event_unregister_' ~ evenement.id) }}">
                    <button type="submit" class="w-full px-6 py-3 rounded-lg border border-[#E5E0D8] text-[#6B7280] font-medium hover:bg-[#F5F1EB] focus:outline focus:ring-2 focus:ring-[#A7C7E7]">Se désinscrire</button>
                </form>
            {% else %}
                <form method="post" action="{{ path('user_event_register', { id: evenement.id }) }}">
                    <input type="hidden" name="_token" value="{{ csrf_token('event_register_' ~ evenement.id) }}">
                    <button type="submit" class="w-full px-6 py-3 rounded-lg bg-[#A7C7E7] text-white font-medium hover:bg-[#B8D4ED] focus:outline focus:ring-2 focus:ring-[#A7C7E7]">S'inscrire à l'événement</button>
                </form>
            {% endif %}
>>>>>>> 72089269acfd37b80d1154606c1f9a5afd193770
        </div>
    </aside>
</div>
{% endblock %}
