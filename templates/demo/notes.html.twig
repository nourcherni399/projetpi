<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Démo PDF Notes - AutiCare</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes slideInUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-slideUp {
            animation: slideInUp 0.6s ease-out;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <div class="max-w-4xl mx-auto animate-slideUp">
            <!-- Header -->
            <div class="text-center mb-12">
                <h1 class="text-4xl font-bold text-gray-800 mb-4">
                    📋 PDF Notes Patients - Données Réelles
                </h1>
                <p class="text-lg text-gray-600">
                    Génération d'un PDF avec les vraies notes de la base de données
                </p>
            </div>

            <!-- Demo Card -->
            <div class="bg-white rounded-2xl shadow-xl p-8 mb-8">
                <div class="flex items-center gap-4 mb-6">
                    <div class="w-12 h-12 rounded-xl bg-gradient-to-br from-blue-500 to-blue-600 text-white flex items-center justify-center shadow-lg">
                        <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                        </svg>
                    </div>
                    <div>
                        <h2 class="text-2xl font-bold text-gray-800">Test PDF Notes</h2>
                        <p class="text-gray-600">Génération d'un PDF avec données de démonstration</p>
                    </div>
                </div>

                <!-- Features -->
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                    <div class="bg-blue-50 rounded-xl p-4">
                        <h3 class="font-semibold text-blue-800 mb-2">👥 Patients Réels</h3>
                        <p class="text-sm text-blue-600">Données de la base de données</p>
                    </div>
                    <div class="bg-green-50 rounded-xl p-4">
                        <h3 class="font-semibold text-green-800 mb-2">📝 Notes Authentiques</h3>
                        <p class="text-sm text-green-600">Contenu médical réel</p>
                    </div>
                    <div class="bg-purple-50 rounded-xl p-4">
                        <h3 class="font-semibold text-purple-800 mb-2">📄 Format A4</h3>
                        <p class="text-sm text-purple-600">Mise en page professionnelle</p>
                    </div>
                </div>

                <!-- Demo Button -->
                <div class="text-center">
                    <a href="{{ path('demo_notes_pdf') }}" 
                       class="inline-flex items-center gap-3 px-8 py-4 rounded-xl bg-gradient-to-r from-blue-500 to-blue-600 text-white font-bold hover:from-blue-600 hover:to-blue-700 shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1 text-lg">
                        <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                        </svg>
                        Télécharger le PDF avec les vraies notes
                    </a>
                    <p class="text-sm text-gray-500 mt-3">
                        Le PDF sera généré avec les vraies notes de la base de données
                    </p>
                </div>
            </div>

            <!-- Existing Notes Display -->
            <div class="bg-white rounded-2xl shadow-xl p-8 mb-8">
                <h3 class="text-xl font-bold text-gray-800 mb-6 flex items-center gap-2">
                    <svg class="w-5 h-5 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                    </svg>
                    Notes existantes
                </h3>
                
                <div class="space-y-4">
                    <div class="p-4 bg-blue-50 rounded-lg">
                        <p class="text-sm text-blue-700">
                            <strong>💡 Information :</strong> Les notes ajoutées via ce formulaire apparaîtront ici et dans le PDF.
                        </p>
                    </div>
                    
                    {% if allNotes is not empty %}
                        {% for note in allNotes %}
                            <div class="bg-gray-50 rounded-lg p-4 hover:bg-gray-100 transition-colors">
                                <div class="flex justify-between items-start mb-2">
                                    <div>
                                        <h4 class="font-semibold text-gray-800">
                                            {{ note.patient ? (note.patient.prenom|default('') ~ ' ' ~ note.patient.nom|default('')) : 'Patient inconnu' }}
                                        </h4>
                                        {% if note.patient.email %}
                                            <p class="text-sm text-gray-600">{{ note.patient.email }}</p>
                                        {% endif %}
                                    </div>
                                    <div class="text-right">
                                        <span class="text-xs text-gray-500">
                                            {{ note.dateCreation ? note.dateCreation|date('d/m/Y H:i') : 'Date inconnue' }}
                                        </span>
                                    </div>
                                </div>
                                <div class="mt-3 p-3 bg-white rounded border border-gray-200">
                                    <p class="text-gray-700 text-sm">{{ note.contenu|nl2br }}</p>
                                </div>
                            </div>
                        {% endfor %}
                    {% else %}
                        <div class="text-center py-8 text-gray-500">
                            <svg class="w-16 h-16 mx-auto mb-4 text-gray-300" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                            </svg>
                            <p>Aucune note enregistrée pour le moment.</p>
                            <p class="text-sm mt-2">Ajoutez votre première note avec le formulaire ci-dessus.</p>
                        </div>
                    {% endif %}
                </div>
            </div>

            <!-- Add Note Form -->
            <div class="bg-white rounded-2xl shadow-xl p-8 mb-8">
                <h3 class="text-xl font-bold text-gray-800 mb-6 flex items-center gap-2">
                    <svg class="w-5 h-5 text-green-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                    </svg>
                    Ajouter une note
                </h3>
                
                <form method="POST" action="{{ path('demo_notes') }}" class="space-y-4">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Nom du patient</label>
                            <input type="text" name="patient_name" required
                                   class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                                   placeholder="Ex: Marie Martin">
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Email du patient (optionnel)</label>
                            <input type="email" name="patient_email"
                                   class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                                   placeholder="patient@email.com">
                        </div>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Contenu de la note</label>
                        <textarea name="content" required rows="4"
                                  class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                                  placeholder="Écrivez votre note médicale ici..."></textarea>
                    </div>
                    
                    <div class="flex justify-end">
                        <button type="submit" 
                                class="px-6 py-3 bg-gradient-to-r from-green-500 to-green-600 text-white font-medium rounded-lg hover:from-green-600 hover:to-green-700 shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1">
                            <span class="flex items-center gap-2">
                                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" />
                                </svg>
                                Ajouter la note
                            </span>
                        </button>
                    </div>
                </form>
                
                <div class="mt-4 p-4 bg-blue-50 rounded-lg">
                    <p class="text-sm text-blue-700">
                        <strong>💡 Astuce :</strong> Les notes ajoutées ici seront enregistrées dans la base de données et apparaîtront dans le PDF.
                    </p>
                </div>
            </div>

            <!-- Structure Preview -->
            <div class="bg-white rounded-2xl shadow-xl p-8">
                <h3 class="text-xl font-bold text-gray-800 mb-6 flex items-center gap-2">
                    <svg class="w-5 h-5 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
                    </svg>
                    Structure du PDF
                </h3>
                
                <div class="space-y-4">
                    <div class="border-l-4 border-blue-500 pl-4">
                        <h4 class="font-semibold text-gray-800">📋 En-tête</h4>
                        <p class="text-sm text-gray-600">Informations du médecin et date de génération</p>
                    </div>
                    
                    <div class="border-l-4 border-green-500 pl-4">
                        <h4 class="font-semibold text-gray-800">👂 Organisation par patient</h4>
                        <p class="text-sm text-gray-600">Notes groupées par patient avec saut de page</p>
                    </div>
                    
                    <div class="border-l-4 border-purple-500 pl-4">
                        <h4 class="font-semibold text-gray-800">📝 Contenu des notes</h4>
                        <p class="text-sm text-gray-600">Date, contenu détaillé et informations patient</p>
                    </div>
                    
                    <div class="border-l-4 border-orange-500 pl-4">
                        <h4 class="font-semibold text-gray-800">🎨 Design professionnel</h4>
                        <p class="text-sm text-gray-600">Mise en page claire avec couleurs et espacement</p>
                    </div>
                </div>
            </div>

            <!-- Instructions -->
            <div class="bg-gradient-to-r from-blue-50 to-indigo-50 rounded-2xl p-6 mt-8">
                <h3 class="text-lg font-bold text-gray-800 mb-3">💡 Instructions</h3>
                <ol class="space-y-2 text-gray-700">
                    <li class="flex items-start gap-2">
                        <span class="text-blue-500 font-bold">1.</span>
                        <span>Cliquez sur le bouton "Télécharger le PDF de démonstration"</span>
                    </li>
                    <li class="flex items-start gap-2">
                        <span class="text-blue-500 font-bold">2.</span>
                        <span>Le PDF s'ouvrira dans votre navigateur ou sera téléchargé</span>
                    </li>
                    <li class="flex items-start gap-2">
                        <span class="text-blue-500 font-bold">3.</span>
                        <span>Consultez la structure et l'organisation des notes patients</span>
                    </li>
                </ol>
            </div>
        </div>
    </div>
</body>
</html>
