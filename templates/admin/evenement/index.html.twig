{% extends 'layouts/admin.html.twig' %}

{% block title %}Événements - AutiCare Admin{% endblock %}

{% block admin_content %}
<h1 class="text-2xl font-semibold text-[#4B5563] mb-6">Gestion des événements</h1>
{% for message in app.flashes('success') %}
    <div class="mb-4 px-4 py-3 rounded-lg bg-emerald-50 border border-emerald-200 text-emerald-800">{{ message }}</div>
{% endfor %}
<<<<<<< HEAD
<div class="bg-white rounded-xl border border-[#E5E0D8] overflow-hidden">
    <div class="p-4 border-b border-[#E5E0D8] flex flex-wrap gap-4 justify-between">
        <input type="search" placeholder="Rechercher un événement…" class="px-4 py-2 rounded-lg border border-[#E5E0D8] bg-[#F5F1EB] text-[#4B5563] placeholder-[#9CA3AF] focus:outline focus:ring-2 focus:ring-[#A7C7E7] max-w-xs">
        <a href="{{ path('admin_evenement_new') }}" class="px-4 py-2 rounded-lg bg-[#A7C7E7] text-white font-medium hover:bg-[#B8D4ED] focus:outline focus:ring-2 focus:ring-[#A7C7E7]">Nouvel élément</a>
    </div>
    <div class="overflow-x-auto">
        <table class="w-full text-left">
            <thead class="bg-[#F5F1EB] border-b border-[#E5E0D8]">
                <tr>
                    <th class="px-6 py-4 font-medium text-[#4B5563]">Titre</th>
                    <th class="px-6 py-4 font-medium text-[#4B5563]">Date</th>
                    <th class="px-6 py-4 font-medium text-[#4B5563]">Lieu</th>
                    <th class="px-6 py-4 font-medium text-[#4B5563]">Thématique</th>
                    <th class="px-6 py-4 font-medium text-[#4B5563]">Actions</th>
=======
{% for message in app.flashes('error') %}
    <div class="mb-4 px-4 py-3 rounded-lg bg-red-50 border border-red-200 text-red-800">{{ message }}</div>
{% endfor %}

<form method="get" action="{{ path('admin_evenement_index') }}" class="bg-white rounded-xl border border-[#E5E0D8] p-4 mb-4">
    <div class="flex flex-wrap gap-4 items-end">
        <div class="flex-1 min-w-[200px]">
            <label for="q" class="block text-sm font-medium text-[#4B5563] mb-1">Recherche</label>
            <input type="search" id="q" name="q" value="{{ q|default('') }}" placeholder="Titre, lieu, thématique…" class="w-full px-4 py-2 rounded-lg border border-[#E5E0D8] bg-[#F5F1EB] text-[#4B5563] placeholder-[#9CA3AF] focus:outline focus:ring-2 focus:ring-[#A7C7E7]">
        </div>
        <div>
            <label for="sort" class="block text-sm font-medium text-[#4B5563] mb-1">Tri</label>
            <select id="sort" name="sort" class="px-4 py-2 rounded-lg border border-[#E5E0D8] bg-white text-[#4B5563] focus:outline focus:ring-2 focus:ring-[#A7C7E7]">
                <option value="date" {{ sortBy|default('date') == 'date' ? 'selected' : '' }}>Date</option>
                <option value="lieu" {{ sortBy|default('date') == 'lieu' ? 'selected' : '' }}>Lieu</option>
                <option value="theme" {{ sortBy|default('date') == 'theme' ? 'selected' : '' }}>Thématique</option>
            </select>
        </div>
        <div>
            <label for="order" class="block text-sm font-medium text-[#4B5563] mb-1">Ordre</label>
            <select id="order" name="order" class="px-4 py-2 rounded-lg border border-[#E5E0D8] bg-white text-[#4B5563] focus:outline focus:ring-2 focus:ring-[#A7C7E7]">
                <option value="asc" {{ sortOrder|default('asc') == 'asc' ? 'selected' : '' }}>Croissant</option>
                <option value="desc" {{ sortOrder|default('asc') == 'desc' ? 'selected' : '' }}>Décroissant</option>
            </select>
        </div>
        <button type="submit" class="px-4 py-2 rounded-lg bg-[#F5F1EB] border border-[#E5E0D8] text-[#4B5563] font-medium hover:bg-[#E5E0D8] focus:outline focus:ring-2 focus:ring-[#A7C7E7]">Filtrer</button>
        <a href="{{ path('admin_evenement_new') }}" class="px-4 py-2 rounded-lg bg-[#A7C7E7] text-white font-medium hover:bg-[#B8D4ED] focus:outline focus:ring-2 focus:ring-[#A7C7E7]">Nouvel élément</a>
    </div>
</form>

<div class="bg-white rounded-2xl border border-[#E5E0D8] shadow-sm overflow-hidden">
    <div class="overflow-x-auto">
        <table class="w-full text-left">
            <thead class="bg-[#F5F1EB] border-b-2 border-[#E5E0D8]">
                <tr>
                    <th class="px-6 py-4 font-semibold text-[#4B5563] text-sm uppercase tracking-wide">Titre</th>
                    <th class="px-6 py-4 font-semibold text-[#4B5563] text-sm uppercase tracking-wide">Date</th>
                    <th class="px-6 py-4 font-semibold text-[#4B5563] text-sm uppercase tracking-wide">Lieu</th>
                    <th class="px-6 py-4 font-semibold text-[#4B5563] text-sm uppercase tracking-wide">Thématique</th>
                    <th class="px-6 py-4 font-semibold text-[#4B5563] text-sm uppercase tracking-wide text-right">Actions</th>
>>>>>>> 72089269acfd37b80d1154606c1f9a5afd193770
                </tr>
            </thead>
            <tbody class="divide-y divide-[#E5E0D8]">
                {% for event in evenements %}
<<<<<<< HEAD
                <tr class="hover:bg-[#F5F1EB]/50">
                    <td class="px-6 py-4 text-[#4B5563]">{{ event.title }}</td>
                    <td class="px-6 py-4 text-[#6B7280]">{{ event.dateEvent ? event.dateEvent|date('d/m/Y') : '—' }} {{ event.heureDebut ? event.heureDebut|date('H:i') : '' }}</td>
                    <td class="px-6 py-4 text-[#6B7280]">{{ event.lieu }}</td>
                    <td class="px-6 py-4 text-[#6B7280]">{{ event.thematique ? event.thematique.nomThematique : '—' }}</td>
                    <td class="px-6 py-4"><span class="text-[#6B7280] text-sm">—</span></td>
                </tr>
                {% else %}
                <tr><td colspan="5" class="px-6 py-8 text-center text-[#6B7280]">Aucun événement.</td></tr>
=======
                <tr class="hover:bg-[#F5F1EB]/60 transition-colors">
                    <td class="px-6 py-4">
                        <span class="font-medium text-[#4B5563]">{{ event.title }}</span>
                    </td>
                    <td class="px-6 py-4 text-[#6B7280] text-sm">{{ event.dateEvent ? event.dateEvent|date('d/m/Y') : '—' }}{% if event.heureDebut %} <span class="text-[#9CA3AF]">{{ event.heureDebut|date('H:i') }}</span>{% endif %}</td>
                    <td class="px-6 py-4 text-[#6B7280] text-sm">{{ event.lieu }}</td>
                    <td class="px-6 py-4">
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-[#A7C7E7]/20 text-[#4B5563]">{{ event.thematique ? event.thematique.nomThematique : '—' }}</span>
                    </td>
                    <td class="px-6 py-4 text-right">
                        <div class="flex flex-wrap gap-2 justify-end">
                            <a href="{{ path('admin_evenement_show', { id: event.id }) }}" class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg bg-[#A7C7E7] text-white text-sm font-medium hover:bg-[#B8D4ED] focus:outline focus:ring-2 focus:ring-[#A7C7E7] focus:ring-offset-1 transition-colors">
                                <svg class="w-4 h-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" /><path stroke-linecap="round" stroke-linejoin="round" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" /></svg>
                                Voir
                            </a>
                            <a href="{{ path('admin_evenement_edit', { id: event.id }) }}" class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg border border-[#E5E0D8] bg-white text-[#4B5563] text-sm font-medium hover:bg-[#F5F1EB] hover:border-[#A7C7E7]/50 focus:outline focus:ring-2 focus:ring-[#A7C7E7] focus:ring-offset-1 transition-colors">
                                <svg class="w-4 h-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" /></svg>
                                Modifier
                            </a>
                            <form method="post" action="{{ path('admin_evenement_delete', { id: event.id }) }}" class="inline" onsubmit="return confirm('Supprimer cet événement ?');">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete_evenement_' ~ event.id) }}">
                                <button type="submit" class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg border border-red-200 bg-white text-red-600 text-sm font-medium hover:bg-red-50 hover:border-red-300 focus:outline focus:ring-2 focus:ring-red-500 focus:ring-offset-1 transition-colors">
                                    <svg class="w-4 h-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" /></svg>
                                    Supprimer
                                </button>
                            </form>
                        </div>
                    </td>
                </tr>
                {% else %}
                <tr><td colspan="5" class="px-6 py-12 text-center text-[#6B7280]">Aucun événement.</td></tr>
>>>>>>> 72089269acfd37b80d1154606c1f9a5afd193770
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
