<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Note - {{ note.patient ? (note.patient.prenom ~ ' ' ~ note.patient.nom) : 'Patient' }}</title>
    <style>
        body { font-family: DejaVu Sans, sans-serif; font-size: 12px; color: #333; margin: 24px; }
        h1 { font-size: 16px; color: #4B5563; border-bottom: 2px solid #A7C7E7; padding-bottom: 8px; }
        .meta { color: #6B7280; font-size: 11px; margin-bottom: 20px; }
        .contenu { white-space: pre-wrap; line-height: 1.5; padding: 12px 0; }
        .footer { margin-top: 32px; font-size: 9px; color: #9CA3AF; }
    </style>
</head>
<body>
    <h1>Note patient</h1>
    <p class="meta">
        Patient : {{ note.patient ? (note.patient.prenom ~ ' ' ~ note.patient.nom) : '—' }}<br>
        Date : {{ note.dateCreation ? note.dateCreation|date('d/m/Y à H:i') : '—' }}<br>
        Dr {{ note.medecin ? (note.medecin.prenom ~ ' ' ~ note.medecin.nom) : '—' }}
    </p>
    <div class="contenu">{{ note.contenu ?? '—' }}</div>
    <p class="footer">AutiCare — Document confidentiel — Exporté le {{ "now"|date("d/m/Y à H:i") }}</p>
</body>
</html>
