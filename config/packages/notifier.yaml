framework:
    notifier:
        chatter_transports:
        texter_transports:
            twilio: '%env(TWILIO_DSN)%'
        channel_policy:
            # email + sms pour les rappels RDV (SmsMessage utilise texter_transports)
            urgent: ['email', 'sms']
            high: ['email', 'sms']
            medium: ['email', 'sms']
            low: ['email', 'sms']
        admin_recipients:
            - { email: admin@example.com }
